# 中国镜像源配置说明

## ✅ 已配置的镜像源

所有项目已配置为中国镜像源，以加速下载。

### 1. Gradle 分发版镜像

**已更新的文件：**
- `gradle/wrapper/gradle-wrapper.properties` - 主项目
- `PlayerBot-Macro-GUI-server/gradle/wrapper/gradle-wrapper.properties` - 服务器项目
- `java17(...)/fabric+forge=1.20.1/gradle/wrapper/gradle-wrapper.properties` - 模组项目

**镜像源：**
- 腾讯云：`https://mirrors.cloud.tencent.com/gradle/`
- 替代原来的：`https://services.gradle.org/distributions/`

**超时时间：**
- 已增加到 60 秒（60000ms）

### 2. Maven 仓库镜像

**已更新的文件：**
- `build.gradle` - 主项目（GUI 客户端）
- `PlayerBot-Macro-GUI-server/build.gradle` - 服务器项目
- `java17(...)/fabric+forge=1.20.1/build.gradle` - 模组项目

**配置的镜像源（按优先级）：**

1. **阿里云 Maven**（主要）
   - URL: `https://maven.aliyun.com/repository/public`
   - 速度最快，推荐使用

2. **腾讯云 Maven**（备用）
   - URL: `https://mirrors.cloud.tencent.com/nexus/repository/maven-public/`
   - 备用镜像

3. **华为云 Maven**（备用，仅模组项目）
   - URL: `https://repo.huaweicloud.com/repository/maven/`
   - 备用镜像

4. **Maven Central**（最后备用）
   - 原始中央仓库
   - 如果所有镜像都失败，会回退到这里

### 3. 全局初始化脚本

**文件：** `init.gradle`

此文件会自动应用到所有 Gradle 构建，配置全局镜像源。

## 🚀 使用方法

### 自动应用

镜像源配置会自动应用，无需额外操作。直接运行编译即可：

```cmd
build-all.bat
```

### 验证配置

编译时会优先使用中国镜像源，下载速度会明显提升。

## 📊 镜像源对比

| 镜像源 | 速度 | 稳定性 | 推荐度 |
|--------|------|--------|--------|
| 阿里云 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ 首选 |
| 腾讯云 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ 备用 |
| 华为云 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⚠️ 备用 |
| Maven Central | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⚠️ 最后备用 |

## 🔧 故障排除

### 问题1：镜像源无法访问

**解决方案：**
- 检查网络连接
- 尝试使用 VPN
- 镜像源会自动回退到下一个

### 问题2：某些依赖找不到

**解决方案：**
- 某些特殊依赖可能只在 Maven Central
- 配置会自动回退到 Maven Central
- 或手动添加到 `repositories` 块

### 问题3：下载仍然很慢

**解决方案：**
1. 检查网络速度
2. 尝试使用其他镜像源
3. 考虑使用代理

## 📝 配置详情

### Gradle Wrapper 配置

```properties
# 腾讯云 Gradle 镜像
distributionUrl=https\://mirrors.cloud.tencent.com/gradle/gradle-8.4-bin.zip
networkTimeout=60000  # 60秒超时
```

### Maven 仓库配置

```gradle
repositories {
    // 阿里云 Maven 镜像（主要）
    maven {
        name "AliyunMaven"
        url "https://maven.aliyun.com/repository/public"
    }
    // 腾讯云 Maven 镜像（备用）
    maven {
        name "TencentMaven"
        url "https://mirrors.cloud.tencent.com/nexus/repository/maven-public/"
    }
    // 原始 Maven Central（最后备用）
    mavenCentral()
}
```

## 🎯 优势

1. **下载速度提升**
   - 中国镜像源通常比国外源快 5-10 倍

2. **稳定性更好**
   - 减少网络超时问题
   - 降低下载失败率

3. **自动回退**
   - 如果镜像源失败，自动使用下一个
   - 确保编译不会因为镜像问题而失败

## ✅ 验证

运行编译脚本，观察下载速度：

```cmd
build-all.bat
```

如果看到从 `maven.aliyun.com` 或 `mirrors.cloud.tencent.com` 下载，说明镜像源配置成功！

## 📚 相关文档

- [阿里云 Maven 镜像](https://developer.aliyun.com/mirror/maven)
- [腾讯云 Maven 镜像](https://mirrors.cloud.tencent.com/)
- [Gradle 镜像配置](https://docs.gradle.org/current/userguide/build_environment.html)

---

**配置完成！** 现在所有项目都会优先使用中国镜像源，下载速度会显著提升！🚀

