# FanMacro 积木式编程 GUI

## 简介

这是一个类似 Scratch 的积木式编程界面，让用户可以通过拖拽积木的方式零成本生成 FanMacro 语言宏文件。

## 功能特性

- 🎨 **积木式编程**：类似 Scratch 的可视化编程界面
- 🖱️ **拖拽操作**：通过拖拽积木来构建程序
- 📝 **实时预览**：实时查看生成的 FanMacro 代码
- 💾 **保存宏文件**：直接保存到 `.minecraft/config/do/` 目录
- 📂 **加载宏文件**：加载已有的宏文件进行编辑
- 🎯 **多种积木类型**：
  - 命令积木（蓝色）：Baritone 命令和原版命令
  - 条件积木（黄色）：if 语句和 check 命令
  - 控制积木（绿色）：wait、end、run 等
  - 函数积木（紫色）：函数定义和调用
  - 事件积木（橙色）：fan_main 主入口点

## 运行要求

- **Java 17 或更高版本**（必需）
- **JavaFX 17**（已包含在 Fat JAR 中，无需单独安装）
- **Gradle**（仅编译时需要，运行 Fat JAR 不需要）

## 编译和运行

### 方法1：使用 Fat JAR（推荐 - 独立运行）

**这是最简单的方法，生成的 JAR 包含所有依赖，可以在任何安装了 Java 17+ 的机器上运行。**

```bash
# 编译项目（生成包含所有依赖的 Fat JAR）
./gradlew build

# Windows: 使用启动脚本
run-gui-jar.bat

# Linux/Mac: 使用启动脚本
chmod +x run-gui-jar.sh
./run-gui-jar.sh

# 或直接运行 JAR 文件
java -jar build/libs/PlayerBot-Macro-GUI-1.0-SNAPSHOT-all.jar
```

**生成的 Fat JAR 文件：**
- 位置：`build/libs/PlayerBot-Macro-GUI-1.0-SNAPSHOT-all.jar`
- 大小：约 12MB（包含所有依赖）
- 特点：可独立运行，无需 Gradle 或额外配置

### 方法2：使用 Gradle

```bash
# 编译项目
./gradlew build

# 运行 GUI
./gradlew run
```

### 方法3：使用启动脚本

**Windows:**
```cmd
run-gui.bat
```

**Linux/Mac:**
```bash
chmod +x run-gui.sh
./run-gui.sh
```

### 方法4：使用 IDE

1. 使用 IntelliJ IDEA 或 Eclipse 打开项目
2. 等待 Gradle 同步完成
3. 运行 `FanMacroGUI` 类的 `main` 方法

## 使用说明

### 基本操作

1. **添加积木**：从左侧积木面板点击积木，会在画布上创建新积木
2. **移动积木**：拖拽积木到画布上的任意位置
3. **连接积木**：将积木靠近其他积木，会自动连接
4. **删除积木**：双击积木或右键选择"删除"
5. **查看代码**：右侧实时显示生成的 FanMacro 代码

### 保存宏文件

1. 在顶部工具栏输入宏名称
2. 点击"保存宏文件"按钮
3. 选择保存目录（默认 `.minecraft/config/do/`）
4. 宏文件将保存为 `.txt` 格式

### 加载宏文件

1. 点击"加载宏文件"按钮
2. 选择要加载的 `.txt` 宏文件
3. 代码预览会更新（注意：反向解析功能待实现）

## 积木类型说明

### 命令积木（蓝色）

- **移动到坐标**：`do #goto x y z;`
- **挖掘矿石**：`do #mine ore;`
- **停止任务**：`do #stop;`
- **允许/禁止破坏**：`do #set allowBreak true/false;`
- **允许/禁止放置**：`do #set allowPlace true/false;`
- **执行原版命令**：`do /home;`、`do /spawn;`

### 条件积木（黄色）

- **如果位置是**：`if me at = (x, y, z)`
- **如果时间是**：`if time = time`、`if time >= time`、`if time <= time`
- **检查有物品**：`check me have (item = item, quantity = qty), do action;`
- **检查无物品**：`check me nothave (item = item), do action;`
- **检查位置**：`check me at = (x, y, z), do action;`
- **检查时间**：`check time = time, do action;`

### 控制积木（绿色）

- **等待**：`wait Xs;`、`wait Xm;`、`wait Xh;`、`wait;`
- **结束**：`do end;`
- **运行宏**：`run name = "macro";`

### 函数积木（紫色）

- **定义函数（前台）**：`fun name="name";`
- **定义函数（后台）**：`fun name="name" type= &;`
- **调用函数**：`do fun "name";`
- **主入口点**：`fan_main:`

## 注意事项

1. **占位符处理**：当前版本中，积木中的占位符（如 `{x}`, `{y}`, `{item}` 等）会使用默认值。未来版本将支持对话框输入。

2. **反向解析**：从代码解析回积木的功能尚未实现，加载宏文件时只会显示代码预览。

3. **积木连接**：当前版本支持简单的积木连接，复杂的嵌套结构（如 if-else）需要手动编辑代码。

4. **文件路径**：默认保存路径为 `.minecraft/config/do/`，如果目录不存在，需要手动选择保存位置。

## 未来计划

- [ ] 实现参数输入对话框
- [ ] 实现代码到积木的反向解析
- [ ] 支持 if-else 嵌套结构的可视化
- [ ] 添加更多积木类型
- [ ] 支持积木的复制和粘贴
- [ ] 添加撤销/重做功能
- [ ] 支持多文件标签页

## 技术栈

- **Java 17**
- **JavaFX 17**：GUI 框架
- **Gradle**：构建工具

## 许可证

本项目为 PlayerBot 模组的一部分。

